
Функция МетодТест(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-type", "text/html");
	Ответ.УстановитьТелоИзСтроки("TELERGAM OK");
	Возврат Ответ;
КонецФункции

Функция ОсновнойМетод(Запрос)
 	
 	УстановитьПривилегированныйРежим(Истина);

	Попытка
		
		тг_Сервер.ПолученВходящийЗапрос(Запрос);
		Возврат тг_Сервер.ВходящийЗапросОтработан();

	Исключение

		Возврат тг_Сервер.ВходящийЗапросОтработан();

	КонецПопытки;	

КонецФункции